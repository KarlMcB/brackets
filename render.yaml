services:
  - type: web
    name: brackets-api
    runtime: node
    rootDir: .
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: FB_CREDENTIALS
        sync: false
      - key: PORT
        value: 3001
